{
  "mcpServers": {
    "harmony-repository-inspector": {
      "command": "uv",
      "args": [
        "run",
        "--project",
        "mcp_server",
        "harmony-repository-mcp"
      ],
      "env": {
        "HARMONY_AGENT_MCP_WORKSPACE": "${HARMONY_AGENT_MCP_WORKSPACE:-.}"
      }
    }
  }
}
